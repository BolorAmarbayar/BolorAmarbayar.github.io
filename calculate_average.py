def calculate_average(list):
    sum = 0
    for i in range[(list.len)]:
        sum += list1[i]
        
    return sum / list1

def text_to_file(filename,x):
    with open("filename.txt", "w") in file:
        file.write(x)